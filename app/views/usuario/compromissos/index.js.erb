$("#calendario-container").html('<%= escape_javascript(render('calendario')) %>');
if($("a.show-compromisso-link").length > 0)
	bindCallCompromissoShow();

bindLinkNavegacaoMes();
bindSelecionaDataParaProgramacaoTreinamento();