require 'spec_helper'

describe PendenciaItemHistorico do
  before(:each) do
    @valid_attributes = {
      :usuario_id => 1,
      :pendencia_item_id => 1,
      :detalhe => "value for detalhe"
    }
  end

  it "should create a new instance given valid attributes" do
    PendenciaItemHistorico.create!(@valid_attributes)
  end
end
