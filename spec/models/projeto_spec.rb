# -*- encoding : utf-8 -*-
require 'spec_helper'

describe Solucao do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Solucao.create!(@valid_attributes)
  end
end
